@model IList<Works.DeveloperEvaluation.WebApi.Features.Livros.ListarLivro.ListarLivroResponse>

@{
    ViewData["Title"] = "Lista de Livros";
}

<h1>Livros</h1>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success">@TempData["Success"]</div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<p>
    <a asp-action="Create" class="btn btn-primary">Criar Novo Livro</a>
</p>

<table class="table table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Preço</th>
            <th>Estoque</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.CodL</td>
                <td>@item.Titulo</td>
                <td>@item.Editora</td>
               @*  <td>@item.Preco.ToString("C")</td> *@
                <td>@item.Edicao</td>
                <td>@item.AnoPublicacao</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.CodL" class="btn btn-sm btn-warning">Editar</a>
                    @* <a asp-action="Details" asp-route-id="@item.CodL" class="btn btn-sm btn-info">Detalhes</a> *@ 
                    <a asp-action="Delete" asp-route-id="@item.CodL" class="btn btn-sm btn-danger">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
```
